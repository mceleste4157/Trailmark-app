# OS / editor cruft
.DS_Store
*.swp

# Local env / secrets (for future Supabase sync)
.env
.env.local

# Native local build state
android/local.properties
android/.gradle/
android/.kotlin/
android/.idea/
android/build/
android/app/build/

# Generated by `xcodegen generate` from ios/project.yml — regenerate,
# don't commit (xcodegen also owns ios/Trailmark/*.xcworkspace if present).
ios/Trailmark.xcodeproj/
ios/*.xcworkspace/
