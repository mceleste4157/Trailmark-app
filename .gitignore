# Offline map tile archives (large binary files, fetched separately per README)
data/regions/*.pmtiles
# ...except the small bundled test fixture (Florence, Italy) used to verify
# the offline-map pipeline works end to end without generating a real region.
!data/regions/firenze-test.pmtiles

# OS / editor cruft
.DS_Store
*.swp

# Local env / secrets (for future Supabase sync)
.env
.env.local
